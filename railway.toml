[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["backend/**", "frontend/**", "Dockerfile", "railway.toml"]

[deploy]
startCommand = "./entrypoint.sh"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
